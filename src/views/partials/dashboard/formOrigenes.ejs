<label>
  <h4>Origen</h4>
  <input type="text" name="origen" id="origen" value="<%= locals.origen ? origen.origen : null %>">
  <span><%= (locals.errors && errors.origen) ? errors.origen.msg : null %></span>
</label>
<%- include(`./submitButton.ejs`) %>